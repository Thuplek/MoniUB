<?xml version="1.0" encoding="utf-8"?>

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.moniub">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logomoniubicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logomoniubicon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EditUserActivity" />
        <activity android:name=".TornarMonitorActivity" />
        <activity
            android:name=".PrincipalActivity"
            android:label="Cursos" />
        <activity
            android:name=".TelaMateriasPeriodo"
            android:label="Materias" />
        <activity
            android:name=".MenuActivity"
             />
        <activity
            android:name=".LoginActivity"
            android:label="Login" />
        <activity
            android:name=".RegisterActivity"
            android:label="Cadastro" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>